function roller(num) {

    window.location.href = `queue-booking/roller/roller${num}.html`;
}

function viking(num) {
    
    window.location.href = `queue-booking/viking/viking${num}.html`;
}

function spin(num) {
    
    window.location.href = `queue-booking/spin/spin${num}.html`;
}